# Lombok이 생성자를 만들 때 필드의 @Qualifier를 생성자 파라미터에도 복사하게 함
# (기본값은 복사 안 함 → @RequiredArgsConstructor + @Qualifier 조합이 무시됨)
lombok.copyableAnnotations += org.springframework.beans.factory.annotation.Qualifier